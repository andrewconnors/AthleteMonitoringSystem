//
//  cAppTests.swift
//  cAppTests
//
//  Created by Andrew Connors on 2016-10-21.
//  Copyright © 2016 Andrew Connors. All rights reserved.
//

import XCTest
@testable import cApp

class cAppTests: XCTestCase {
    
    override func setUp() {
        super.setUp()
        // Put setup code here. This method is called before the invocation of each test method in the class.
    }
    
    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }
    
    func createPlayerTest() {
        // Use XCTAssert and related functions to verify your tests produce the correct results.
//        let person = Athlete()
//        person.name = "RandomName"
//        
//        XCTAssertTrue(person.getName("RandomName"))
        
    }
    
    func addPlayerToDatabaseTest() {
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        
    }
    func createTeamTest() {
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        
    }
    func addTeamToDatabaseTest() {
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        
    }
    
    func createSensorTest() {
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        
    }
    
    func addSensorToDatabaseTest() {
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        
    }
    
    func assignSensorToPlayerTest() {
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        
    }
    
    func removeFromDatabaseTest() {
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        
    }
    
    func testPerformanceExample() {
        // This is an example of a performance test case.
        
    }
    
}
